{
  "mcpServers": {
    "jockey": {
      "type": "http",
      "url": "${JOCKEY_MCP_URL:-https://mcp.twelvelabs.io/jockey/mcp}",
      "auth": {
        "type": "oauth"
      },
      "notes": "Jockey is the central media library. Do not hardcode secrets. Configure OAuth or environment variables locally."
    },
    "figwright": {
      "type": "http",
      "url": "${FIGWRIGHT_MCP_URL}",
      "auth": {
        "type": "oauth"
      },
      "notes": "Set FIGWRIGHT_MCP_URL in the local environment or replace with the approved project MCP URL. No tokens in repo."
    },
    "apify": {
      "type": "http",
      "url": "${APIFY_MCP_URL}",
      "headers": {
        "Authorization": "Bearer ${APIFY_TOKEN}"
      },
      "notes": "Set APIFY_MCP_URL and APIFY_TOKEN outside the repo. Never commit real tokens."
    }
  }
}
